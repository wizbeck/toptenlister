%h1= @list.title
%h3 Description:
%p= @list.description
%ol
  - @list.items.each do |item|
    - if !item.blank?
      %li= item
      %br/
- if @list.user == current_user
  %h4
    = link_to 'Edit List', edit_list_path(@list)
    | #{link_to 'Delete List', list_path(@list), method: :delete, data: { confirm: "Are you sure?" }}
%p
  For more lists of the #{@list.topic.name} topic click #{link_to 'here', topic_lists_path(@list.topic)}
