<h1>Topic: <%= @topic.name %></h1>

Top <%= @topic.name %> lists:

<%@topic.lists.each do |list| %>
  <ul>
    <li><%= link_to list.title, list_path(list) %></li>
  </ul>
<% end %>

<p> Make a new Top <%=link_to @topic.name, new_topic_list_path(@topic) %> </p>